# Pure Python dependencies synchronized into ~/ipod-tools/venv by uv.
#
# Two, deliberately. GTK4 is a C library and PyGObject needs GObject
# Introspection and Cairo development headers when built from PyPI. install.sh
# instead bases the environment on the distro Python, exposes its site packages,
# and installs the smaller, tested python3-gi runtime package separately.

# Artist and album metadata, which the database builder writes into iTunesDB.
mutagen>=1.45

# Used by ipod-fetch.sh. Distro packages of yt-dlp go stale quickly and stop
# working when YouTube changes, so this project owns a copy it can update
# itself through ./ipod-fetch.sh --update.
yt-dlp>=2025.11.12
